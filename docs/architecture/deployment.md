# Deployment architecture

The built JavaScript module is published as a GitHub release asset and installed through
HACS or copied manually to Home Assistant. The browser loads the module from the Home
Assistant web server. No separate backend service is deployed.

See the [deployment diagram](diagrams/deployment.md).
