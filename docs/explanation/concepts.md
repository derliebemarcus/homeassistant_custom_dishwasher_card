# Concepts

The card is a standalone Home Assistant frontend module. It discovers Home Connect
entities from a configured device, normalizes their states, and presents appliance
status and controls without depending on another custom card.

The source and generated distribution file are intentionally versioned together so HACS
and GitHub release consumers receive exactly the artifact validated by CI.
